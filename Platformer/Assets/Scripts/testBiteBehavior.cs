using HoudiniEngineUnity;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testBiteBehavior : MonoBehaviour
{
    public HEU_HoudiniAssetRoot HEUcontrols;
    void Start()
    {
        HEUcontrols = GetComponent<HEU_HoudiniAssetRoot>();
    }

    // Update is called once per frame
    void Update()
    {
 
    }
}
